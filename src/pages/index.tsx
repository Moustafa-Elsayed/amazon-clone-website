import Image from "next/image";
import { Inter } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });

export default function Home() {
  return (
    <h1 className="flex items-center justify-center bg-black h-full  text-white">
      mostafa elsayed
    </h1>
  );
}
